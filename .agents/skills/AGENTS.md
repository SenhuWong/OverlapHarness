# Project-Local Skill Maintenance

- Keep every required CLI and deterministic helper below its owning
  `.agents/skills/<skill>/` directory.
- Keep each local skill self-contained; do not depend on unrelated repository
  scripts or mutable runtime artifacts.
